import React from 'react';
import { withLayout } from '../common';
import StudyContainer from './StudyContainer';

const StudyPage = () => {
  return <StudyContainer />;
};

export default withLayout(StudyPage);
